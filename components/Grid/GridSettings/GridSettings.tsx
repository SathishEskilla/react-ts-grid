import * as React from 'react';
import { GridDataSourcePropsType } from '../../../types/global-types';

/**
 * A sub-component for Grid to read data.
 */
const GridSettings: React.FC<{
  dataSource: GridDataSourcePropsType[];
}> = () => {
  return <React.Fragment />;
};

export default GridSettings;
